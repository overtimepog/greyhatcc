---
description: Check if a bug has been previously found, reported, or submitted before writing a report
aliases:
  - dup
  - duplicate
  - check
---

# Duplicate Finding Checker

Invoke the `dedup-checker` skill with: {{ARGUMENTS}}

Usage:
- `/greyhatcc:dedup "CORS misconfiguration on api.example.com"` - Check for duplicates
