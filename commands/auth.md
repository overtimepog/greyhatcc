---
description: Test OAuth, JWT, OIDC, SAML, and authentication flows for token manipulation and bypass
aliases:
  - jwt
  - oauth
  - token
---

# Authentication Flow Testing

Invoke the `oauth-jwt-testing` skill for target: {{ARGUMENTS}}
